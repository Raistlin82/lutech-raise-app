# Environment variables for E2E test mode
# This file is loaded when running: npm run dev -- --mode test

# Enable test mode with mock authentication
VITE_TEST_MODE=true

# NO Supabase configuration - forces app to use localStorage for E2E tests
# This ensures tests don't depend on external database connections
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=

# IAS configuration (not used in test mode, but prevents errors)
VITE_IAS_AUTHORITY=https://mock-ias.test
VITE_IAS_CLIENT_ID=mock-client-id
