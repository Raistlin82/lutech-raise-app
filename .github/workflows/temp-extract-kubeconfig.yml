name: TEMP - Extract Kubeconfig

on:
  workflow_dispatch:

jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - name: Display kubeconfig
        run: |
          echo "========== KUBECONFIG CONTENT =========="
          echo "${{ secrets.KYMA_KUBECONFIG }}" | base64 -d
          echo "========================================"
          echo ""
          echo "⚠️  COPY THE ABOVE AND DELETE THIS WORKFLOW RUN!"
